---
id: EXP-PAT-0005
type: EXPERIENCE
tree: experiences
layer: L3
status: ACTIVE
created: 2026-05-01
heartbeat: '2026-05-05'
priority: HIGH
owner: claude-code
tags:
- self-evolution
- methodology
- ordering
- risk-management
- infrastructure
source: PROJ-EVOL-0001
originSessionId: a7664e14-d841-48de-acfa-f9d0da11efe2
---
# 自我进化执行方法论

## 触发条件
执行系统性自我进化（多方向并行改进）时。

## 行动
按以下优先级排序：

### 第一轮：互补配对（零风险）
选择两个互不依赖但互补的方向先行。例如：
- Stop hook（退出保存）+ SessionStart hook（启动加载）= 完整生命周期闭环
- 两者都是纯增量，不改任何现有逻辑，零破坏性

### 第二轮：高价值独立项（需验证）
最有价值但独立的方向次之。例如：
- MCP Server（价值最高，但需协议兼容性验证）
- 先完成再测试，不与其他方向耦合

### 第三轮：锦上添花（低优先级）
摩擦优化最后做。例如：
- 权限白名单优化
- Shell 别名
- 这些改了立刻见效，但不影响核心架构

### 每轮结束后的检查点
1. `mf status` — 森林是否完好
2. `mf health` — 有无引入问题
3. 验证 settings.json 仍是合法 JSON
4. 确认 hooks 能正常执行
5. 更新文档 + 提炼经验

## 结果
四个进化方向在单次会话内完成，零回滚，零破坏。执行顺序避免了同时引入多个变量导致的调试困难。

## 反例
- 四个方向同时开工，交错调试 → 问题来源不明，修改相互覆盖
- 从最难的方向（MCP）开始 → 卡住后整个进化停滞
- 跳过每轮检查点 → 问题累积到最后才发现
- 做完不提炼经验 → 下次进化从零开始

## 关键要点
- **互补先行**：选择天然形成闭环的方向配对（如 start + stop）
- **隔离高风险**：把最复杂的独立项放中间，不和任何东西耦合
- **每轮收敛**：完成一轮→检查→提炼→再开下一轮
- **基础设施边界**：自我迭代只碰工具/配置/脚本，不碰用户代码逻辑（这是 EXP-CORE-0005 的范围限定）

## 适用场景
- 任何需要多个方向并行改进的系统性重构
- AI 自我优化的执行框架
- 跨多个配置文件的自动化改造

## 来源
从 PROJ-EVOL-0001 (首次自我进化) 提炼。四个进化方向 A+B+C+D 在单次会话中完成，执行顺序 A+C → B → D 被验证为正确策略。
